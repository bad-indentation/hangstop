[package]
name = "hangstop"
version = "0.1.1"
edition = "2024"
description = "Blazingly fast Hangman solver using information theory"
license = "MIT"
repository = "https://github.com/bad-indentation/hangstop"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
